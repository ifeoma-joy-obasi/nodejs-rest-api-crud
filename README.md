Vanilla Node.js REST API (No Express)

A pure Node.js RESTful API for managing products with full CRUD functionality, input validation, JSON file storage, and consistent error handling.
Built without Express as part of a backend assignment.

🚀 How to Run the Project

Install dependencies:

npm install


Start the server:
npm start - node server.js
npm run dev - nodemon server.js


Server runs at:
http://localhost:5000

📌 Available Endpoints

GET /api/products → Get all products

GET /api/products/:id → Get single product

POST /api/products → Create a product

PUT /api/products/:id → Update a product

DELETE /api/products/:id → Delete a product

PUT /api/products/:id → Update a product

DELETE /api/products/:id → Delete a product
